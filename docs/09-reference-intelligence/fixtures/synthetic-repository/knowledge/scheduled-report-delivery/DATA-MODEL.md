# Data Model

The synthetic definition implies a schedule record, generation run, generated artifact, and notification reference. It specifies workspace scope, state, failure metadata, private storage, and expiring access.

Exact fields, relationships, database constraints, indexes, cache behavior, soft deletion, and retention periods are intentionally unknown. The object records those gaps rather than inventing a schema.

See [Evidence](EVIDENCE.md).
