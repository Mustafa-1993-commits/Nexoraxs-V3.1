# Dependencies

Upstream concepts are workspace membership, report access, scheduling, a queue, private object storage, and notification delivery. The requesting UI and recipient notification are downstream consumers.

No extension seam, integration provider, or feature flag is defined. The object does not infer one.

See [Evidence](EVIDENCE.md).
